# Configurações de Produção - app.grupoggv.com
NODE_ENV=production
VITE_APP_DOMAIN=app.grupoggv.com
VITE_API_BASE_URL=https://app.grupoggv.com/api
VITE_CALLS_API_BASE=https://app.grupoggv.com/api

# Feature flags
# true = mostra placeholder e NÃO chama RPC; false = renderiza dados reais
VITE_CALLS_UNDER_DEV=true

# Supabase (manter as mesmas)
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# Webhooks
VITE_PIPEDRIVE_WEBHOOK_URL=https://app.grupoggv.com/api/webhook/diag-ggv-register
VITE_N8N_WEBHOOK_URL=https://app.grupoggv.com/api/webhook/diag-ggv-register

# N8N Automation
VITE_N8N_BASE_URL=https://automation-test.ggvinteligencia.com.br

# Gmail (manter as mesmas)
VITE_GMAIL_CLIENT_ID=${GMAIL_CLIENT_ID}
VITE_GMAIL_CLIENT_SECRET=${GMAIL_CLIENT_SECRET}
VITE_GMAIL_REFRESH_TOKEN=${GMAIL_REFRESH_TOKEN}

# ============================================================
# Sentry - Error Tracking & Monitoring
# ============================================================
# Frontend DSN (variável VITE_ para ser incluída no bundle)
VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Sourcemaps upload (build time - usadas pelo @sentry/vite-plugin)
SENTRY_AUTH_TOKEN=sntrys_xxx
SENTRY_ORG=sua-org
SENTRY_PROJECT=ggv-plataforma

# Server-side DSN (para Netlify Functions)
SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# ============================================================
# Google Chat (Netlify Functions)
# ============================================================
# Defina estes valores no painel de variáveis de ambiente (Netlify/GitHub), nunca em código

# Webhook URL para alertas em espaço fixo (existente)
GOOGLE_CHAT_WEBHOOK_URL=

# Google Chat API - Service Account para DMs diretas (OKR notifications)
# Opcao 1 (pode estourar limite de env vars em Functions):
# Base64-encoded JSON do service account: base64 -w0 service-account.json
GOOGLE_CHAT_SERVICE_ACCOUNT_JSON=
# Opcao 2 (recomendado): usar Supabase Storage para evitar limite de 4KB
# Crie um bucket privado (ex: "secrets") e suba o JSON.
GOOGLE_CHAT_SERVICE_ACCOUNT_BUCKET=secrets
GOOGLE_CHAT_SERVICE_ACCOUNT_OBJECT=google-chat-service-account.json

# App domain para links nas mensagens (opcional, default: app.grupoggv.com)
APP_DOMAIN=app.grupoggv.com
